This utility is no longer maintained in this git repository.

It moved into the v4l-utils git repository as that allows
regular releases and has various other advantages.

The v4l-utils git repository is found here:

https://git.linuxtv.org/v4l-utils.git/
